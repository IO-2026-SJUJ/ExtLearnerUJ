sequenceDiagram
    actor A as Administrator
    participant UI as Panel Administracyjny
    participant S as System
    participant SS as Serwis Statystyk
    participant DB as Baza Danych

    A->>UI: Otwiera panel statystyk systemowych
    UI->>S: Żądanie statystyk systemu
    S->>SS: Pobierz statystyki systemowe

    SS->>DB: Pobierz dane o użytkownikach (liczba, aktywni, nowi)
    DB-->>SS: Zwraca dane użytkowników
    SS->>DB: Pobierz dane o moderatorach (liczba, aktywność, oceny)
    DB-->>SS: Zwraca dane moderatorów
    SS->>DB: Pobierz dane o materiałach (dodane, zweryfikowane, odrzucone)
    DB-->>SS: Zwraca dane materiałów
    SS->>DB: Pobierz dane o zgłoszeniach (otwarte, rozpatrzone)
    DB-->>SS: Zwraca dane zgłoszeń
    SS->>DB: Pobierz dane finansowe (płatności, przychody)
    DB-->>SS: Zwraca dane finansowe

    SS->>SS: Kompiluje raporty i wskaźniki
    SS-->>S: Kompletne statystyki systemowe

    S-->>UI: Prezentuje statystyki
    UI-->>A: Wyświetla dashboard z raportami i wskaźnikami

    A->>A: Analizuje raporty i wskaźniki systemu
